# 📡 API 配置与 Prompt 调优指南

> 文档版本：v1.0  
> 更新日期：2026-01

---

## 目录

- [1. 信息源 API 配置](#1-信息源-api-配置)
  - [1.1 RSS 源配置](#11-rss-源配置)
  - [1.2 Web3 爬虫配置](#12-web3-爬虫配置)
  - [1.3 代理配置](#13-代理配置)
- [2. 推送渠道 API 配置](#2-推送渠道-api-配置)
  - [2.1 企业微信 Webhook](#21-企业微信-webhook)
  - [2.2 Telegram Bot](#22-telegram-bot)
  - [2.3 飞书 Webhook](#23-飞书-webhook)
- [3. AI API 配置](#3-ai-api-配置)
  - [3.1 OpenAI API](#31-openai-api)
  - [3.2 Claude API](#32-claude-api)
  - [3.3 本地模型配置](#33-本地模型配置)
- [4. Prompt 调优过程](#4-prompt-调优过程)
  - [4.1 新闻降噪 Prompt 演进](#41-新闻降噪-prompt-演进)
  - [4.2 摘要生成 Prompt 演进](#42-摘要生成-prompt-演进)
  - [4.3 重要性评分 Prompt 演进](#43-重要性评分-prompt-演进)
- [5. 配置最佳实践](#5-配置最佳实践)
- [6. 故障排查](#6-故障排查)

---

## 1. 信息源 API 配置

### 1.1 RSS 源配置

配置文件路径：`src/backend/TrendRadar/config/config.yaml`

#### 当前已接入的 RSS 源（15 个）

| ID | 名称 | RSS URL | 状态 |
|---|---|---|---|
| `cointelegraph` | Cointelegraph | https://cointelegraph.com/rss | ✅ 正常 |
| `coindesk` | CoinDesk | https://www.coindesk.com/arc/outboundfeeds/rss/ | ✅ 正常 |
| `theblock` | The Block | https://www.theblock.co/rss.xml | ✅ 正常 |
| `decrypt` | Decrypt | https://decrypt.co/feed | ✅ 正常 |
| `bitcoinmagazine` | Bitcoin Magazine | https://bitcoinmagazine.com/feed | ✅ 正常 |
| `blockworks` | Blockworks | https://blockworks.co/feed | ✅ 正常 |
| `techflow` | 深潮 TechFlow | https://techflowpost.substack.com/feed | ✅ 正常 |
| `cryptoslate` | CryptoSlate | https://cryptoslate.com/feed/ | ✅ 正常 |
| `newsbtc` | NewsBTC | https://www.newsbtc.com/feed/ | ✅ 正常 |
| `cryptopotato` | CryptoPotato | https://cryptopotato.com/feed/ | ✅ 正常 |
| `beincrypto` | BeInCrypto | https://beincrypto.com/feed/ | ✅ 正常 |
| `utoday` | U.Today | https://u.today/rss | ✅ 正常 |
| `coingape` | CoinGape | https://coingape.com/feed/ | ✅ 正常 |
| `bitcoinist` | Bitcoinist | https://bitcoinist.com/feed/ | ⚠️ 解析异常 |
| `coinpedia` | CoinPedia | https://coinpedia.org/feed/ | ✅ 正常 |

#### RSS 源配置格式

```yaml
rss:
  enabled: true
  feeds:
    - id: "cointelegraph"           # 唯一标识符
      name: "Cointelegraph"         # 显示名称
      url: "https://cointelegraph.com/rss"  # RSS 订阅地址
      max_age_days: 3               # 最大文章年龄（天）
      enabled: true                 # 是否启用
```

#### 添加新 RSS 源

1. 验证 RSS URL 是否有效：
   ```bash
   curl -I "https://example.com/feed"
   ```

2. 在 `config.yaml` 的 `rss.feeds` 下添加配置

3. 运行测试：
   ```bash
   python run_web3_push.py --test
   ```

### 1.2 Web3 爬虫配置

配置文件路径：`src/backend/TrendRadar/config/config.yaml` 和 `web3_sources.yaml`

#### 当前已接入的爬虫源（2 个）

| ID | 名称 | 爬虫类型 | 状态 |
|---|---|---|---|
| `chaincatcher` | ChainCatcher 链捕手 | HTML 解析 | ✅ 正常 |
| `menews` | ME News | JSON API | ❌ SSL 证书过期 |

#### 爬虫配置格式

```yaml
web3:
  enabled: true
  request_interval: 3000    # 请求间隔（毫秒）
  timeout: 30               # 请求超时（秒）
  use_proxy: false          # 是否使用代理
  proxy_url: ""             # 代理地址
  
  freshness_filter:
    enabled: true           # 启用新鲜度过滤
    max_age_days: 3         # 最大文章年龄
  
  feeds:
    - id: "chaincatcher"
      name: "ChainCatcher 链捕手"
      url: "https://www.chaincatcher.com/news"
      crawler_type: "chaincatcher"  # 爬虫类型
      max_items: 50
      enabled: true
      max_age_days: 3
```

#### 支持的爬虫类型

| crawler_type | 说明 | 实现文件 |
|---|---|---|
| `chaincatcher` | ChainCatcher 专用爬虫 | `crawler/web3/chaincatcher.py` |
| `menews` | ME News 专用爬虫 | `crawler/web3/menews.py` |

### 1.3 代理配置

如果目标网站需要代理访问，在 `web3` 配置中设置：

```yaml
web3:
  use_proxy: true
  proxy_url: "http://127.0.0.1:7890"  # 本地代理
  # 或使用 SOCKS5 代理
  # proxy_url: "socks5://127.0.0.1:1080"
```

---

## 2. 推送渠道 API 配置

### 2.1 企业微信 Webhook

#### 获取 Webhook URL

1. 在企业微信中创建群聊
2. 右键群聊 → 添加群机器人 → 新建机器人
3. 复制 Webhook 地址

#### 配置示例

```yaml
notification:
  enabled: true
  channels:
    wework:
      enabled: true
      webhook_url: "https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=YOUR_KEY"
      msg_type: "markdown"  # 支持 markdown / text
```

#### 消息格式限制

- **Markdown 消息**：最大 4096 字节
- **文本消息**：最大 2048 字节
- **每分钟限制**：20 条

#### 测试命令

```bash
curl 'https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=YOUR_KEY' \
  -H 'Content-Type: application/json' \
  -d '{"msgtype":"text","text":{"content":"测试消息"}}'
```

### 2.2 Telegram Bot

#### 创建 Bot

1. 在 Telegram 搜索 `@BotFather`
2. 发送 `/newbot` 创建机器人
3. 记录返回的 Bot Token
4. 将 Bot 添加到目标群组
5. 获取 Chat ID（可通过 `@userinfobot` 获取）

#### 配置示例

```yaml
notification:
  enabled: true
  channels:
    telegram:
      enabled: true
      bot_token: "123456789:ABCdefGHIjklMNOpqrsTUVwxyz"
      chat_id: "-1001234567890"  # 群组 ID 以 -100 开头
```

#### 消息格式限制

- **单条消息**：最大 4096 字符
- **每秒限制**：30 条（群组）/ 1 条（私聊）

#### 测试命令

```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/sendMessage" \
  -d "chat_id=<CHAT_ID>" \
  -d "text=测试消息" \
  -d "parse_mode=HTML"
```

### 2.3 飞书 Webhook

#### 获取 Webhook URL

1. 在飞书群聊中点击设置
2. 群机器人 → 添加机器人 → 自定义机器人
3. 复制 Webhook 地址

#### 配置示例

```yaml
notification:
  enabled: true
  channels:
    feishu:
      enabled: true
      webhook_url: "https://open.feishu.cn/open-apis/bot/v2/hook/YOUR_HOOK_ID"
      secret: "YOUR_SECRET"  # 可选，签名验证
```

---

## 3. AI API 配置

### 3.1 OpenAI API

用于智能降噪、摘要生成等功能。

#### 配置示例

```yaml
ai:
  enabled: true
  provider: "openai"
  openai:
    api_key: "sk-xxxxxxxxxxxxxxxxxxxxxxxx"
    api_base: "https://api.openai.com/v1"  # 可替换为代理地址
    model: "gpt-4o-mini"  # 推荐使用，性价比高
    max_tokens: 2000
    temperature: 0.3  # 降低随机性，保持输出稳定
```

#### 模型选择建议

| 模型 | 适用场景 | 成本 |
|---|---|---|
| `gpt-4o` | 高质量摘要、复杂分析 | 高 |
| `gpt-4o-mini` | 日常降噪、简单摘要 | 低 |
| `gpt-3.5-turbo` | 大批量处理 | 最低 |

### 3.2 Claude API

#### 配置示例

```yaml
ai:
  enabled: true
  provider: "anthropic"
  anthropic:
    api_key: "sk-ant-xxxxxxxxxxxxxxxx"
    model: "claude-3-5-sonnet-20241022"
    max_tokens: 2000
```

### 3.3 本地模型配置

支持 Ollama 等本地模型服务。

#### 配置示例

```yaml
ai:
  enabled: true
  provider: "ollama"
  ollama:
    api_base: "http://localhost:11434"
    model: "llama3.1:8b"
    max_tokens: 2000
```

---

## 4. Prompt 调优过程

### 4.1 新闻降噪 Prompt 演进

#### V1.0 - 基础版本（问题：过滤不精准）

```text
请过滤以下新闻列表中的垃圾信息。
```

**问题**：过于简单，AI 不知道什么是"垃圾信息"，导致误过滤或漏过滤。

#### V1.1 - 添加规则定义

```text
请过滤以下新闻列表中的垃圾信息。

垃圾信息包括：
- 空投信息
- 广告推广
- 低质量内容
```

**问题**：规则仍不够具体，"低质量内容"定义模糊。

#### V1.2 - 细化关键词

```text
你是 Web3 信息过滤专家。请分析以下新闻列表，执行降噪处理：

过滤规则（包含以下关键词的内容应被过滤）：
- 空投相关：空投、airdrop、免费领取、白嫖、撸毛
- 广告推广：限时优惠、注册送、邀请码、推广链接、返佣
- 低质量：暴富、财富密码、百倍币、千倍币、稳赚、躺赚

保留规则（优先级高于过滤）：
- 融资、投资、收购、IPO
- 漏洞、攻击、安全
- 监管、政策、法规

请对每条新闻标记：✅ 保留 / ❌ 过滤，并说明原因。
```

**改进**：准确率提升至 ~85%

#### V2.0 - 当前生产版本（准确率 ~95%）

```text
你是专业的 Web3 资讯编辑，负责为机构投资者筛选高价值信息。

## 任务
分析以下新闻列表，识别并过滤低价值内容。

## 过滤标准（满足任一条件即过滤）

### 1. 空投/撸毛类
关键词：空投、airdrop、免费领取、白嫖、撸毛、claim、free mint
特征：标题包含"教程"、"攻略"配合上述关键词

### 2. 广告推广类
关键词：限时优惠、注册送、邀请码、推广链接、返佣、KOL 推荐
特征：包含明显的利益诱导

### 3. 低质量内容
关键词：暴富、财富密码、百倍币、千倍币、稳赚、躺赚、别错过
特征：标题党、情绪化表达、无实质信息

### 4. 重复/聚合类
特征：多条新闻内容高度相似，仅保留来源最权威的一条

## 保留标准（优先级高于过滤）

### 高优先级（必须保留）
- 融资动态：融资、投资、收购、IPO、估值
- 安全事件：漏洞、攻击、被盗、安全警告
- 监管政策：SEC、监管、政策、合规、牌照

### 中优先级
- 技术更新：主网、升级、发布、上线
- 市场数据：ETF、持仓、流入流出

## 输出格式

对每条新闻：
[序号] ✅/❌ | 新闻标题
理由：简要说明（10字内）

最后统计：
- 保留：X 条
- 过滤：X 条
- 过滤率：XX%
```

**调优要点**：
1. 设定角色（机构投资者视角）
2. 分类明确（4 类过滤 + 2 类保留）
3. 提供特征描述，不仅限于关键词
4. 要求输出理由，便于验证和迭代
5. 明确优先级（保留 > 过滤）

### 4.2 摘要生成 Prompt 演进

#### V1.0 - 基础版本

```text
请总结以下新闻。
```

**问题**：输出长度不可控，格式不统一。

#### V1.5 - 添加约束

```text
请用一句话（不超过 50 字）总结以下新闻的核心信息。
```

**问题**：有时丢失关键数据。

#### V2.0 - 当前生产版本

```text
你是专业的 Web3 资讯编辑。请为以下新闻生成摘要。

## 要求
1. 长度：30-50 字
2. 必须包含的信息（如有）：
   - 涉及的项目/公司名称
   - 关键数字（金额、百分比、日期）
   - 核心事件（融资/上线/攻击等）
3. 禁止：
   - 使用"据悉"、"据了解"等表述
   - 添加主观评价
   - 包含原文没有的信息

## 输出
仅输出摘要内容，不要有其他说明。

---
新闻内容：
{content}
```

### 4.3 重要性评分 Prompt 演进

#### V1.0 - 简单评分

```text
请对以下新闻打分（1-10 分）。
```

**问题**：评分标准不统一，结果难以复现。

#### V2.0 - 当前生产版本

```text
你是 Web3 行业分析师，请对以下新闻进行重要性评分。

## 评分维度（总分 10 分）

### 市场影响力（0-3 分）
- 涉及 BTC/ETH 等主流资产：+2 分
- 可能影响整体市场走势：+1 分

### 资金规模（0-2 分）
- 融资金额 > 1 亿美元：+2 分
- 融资金额 > 1000 万美元：+1 分

### 安全相关（0-3 分）
- 重大安全漏洞/攻击事件：+3 分
- 一般安全提醒：+1 分

### 监管动态（0-2 分）
- SEC/主要国家政策：+2 分
- 其他监管动态：+1 分

## 输出格式

评分：X/10
维度得分：市场(X) + 资金(X) + 安全(X) + 监管(X)
理由：一句话说明（20 字内）
摘要：核心信息（30 字内）

---
新闻：
{content}
```

---

## 5. 配置最佳实践

### 5.1 安全配置

**❌ 错误做法**：直接在配置文件中写入 API Key

```yaml
# 不要这样做！
openai:
  api_key: "sk-1234567890abcdef"
```

**✅ 正确做法**：使用环境变量

```yaml
openai:
  api_key: "${OPENAI_API_KEY}"
```

或在代码中读取：
```python
import os
api_key = os.environ.get("OPENAI_API_KEY")
```

### 5.2 性能优化

1. **RSS 抓取**
   - 设置合理的 `max_age_days`（推荐 3 天）
   - 避免重复抓取（使用缓存）

2. **AI 调用**
   - 批量处理新闻，减少 API 调用次数
   - 使用流式输出（streaming）提升体验
   - 缓存相似内容的处理结果

3. **推送优化**
   - 合并消息，避免频繁推送
   - 设置推送间隔（避免被限流）

### 5.3 监控告警

建议监控以下指标：

| 指标 | 正常范围 | 告警阈值 |
|---|---|---|
| RSS 抓取成功率 | > 90% | < 80% |
| 单次抓取耗时 | < 60s | > 120s |
| AI API 响应时间 | < 5s | > 10s |
| 推送成功率 | 100% | < 95% |

---

## 6. 故障排查

### 6.1 常见错误

#### RSS 抓取失败

```
[RSS] Bitcoinist: 未知错误: 'str' object has no attribute 'get'
```

**原因**：RSS feed 格式不规范  
**解决**：检查 RSS URL 是否有效，或在代码中添加容错处理

#### SSL 证书错误

```
SSLError: certificate verify failed: certificate has expired
```

**原因**：目标网站 SSL 证书过期  
**解决**：
1. 等待网站修复
2. 临时禁用 SSL 验证（不推荐）：
   ```python
   requests.get(url, verify=False)
   ```

#### 推送失败

```
[FAIL] 企业微信推送失败: invalid webhook url
```

**原因**：Webhook URL 配置错误或已过期  
**解决**：重新生成 Webhook URL 并更新配置

### 6.2 调试命令

```bash
# 测试模式（不推送）
python run_web3_push.py --test

# 预览模式（显示将要推送的内容）
python run_web3_push.py --dry-run

# 不保存文件
python run_web3_push.py --no-save

# 不自动打开浏览器
python run_web3_push.py --no-open
```

### 6.3 日志分析

日志格式：`[模块名] 消息`

```
[RSS] 开始抓取 15 个 RSS 源...
[RSS] Cointelegraph: 获取 30 条
[Web3] ChainCatcher 链捕手: 获取 45 条
[SAVE] HTML 报告已保存: output/web3/...
[OK] 推送完成 ✅
```

---

## 附录

### A. 配置文件清单

| 文件 | 用途 |
|---|---|
| `config/config.yaml` | 主配置文件 |
| `config/web3_sources.yaml` | Web3 爬虫详细配置 |

### B. 相关文档

- [选型报告](./selection-report.md)
- [Prompt 合集](./prompts.md)
- [开发设计文档](./develop/design.md)

### C. 更新记录

| 日期 | 版本 | 变更内容 |
|---|---|---|
| 2026-01-10 | v1.0 | 初始版本，包含 15 个 RSS 源 |

---

*如有问题或建议，欢迎提交 Issue！*