# ═══════════════════════════════════════════════════════════════
#                    TrendRadar 配置文件
# ═══════════════════════════════════════════════════════════════

# ===============================================================
# 1. 基础设置
# ===============================================================
app:
  # 时区配置（影响所有时间显示、推送窗口判断、数据存储）
  # 常用时区：
  #   - Asia/Shanghai (北京时间 UTC+8)
  #   - America/New_York (美东时间 UTC-5/-4)
  #   - Europe/London (伦敦时间 UTC+0/+1)
  # 完整时区列表: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
  timezone: "Asia/Shanghai"
  show_version_update: true # 显示版本更新提示

# ===============================================================
# 2. 数据源 - 热榜平台
#
# id: 平台唯一标识（勿修改）
# name: 显示名称（可自定义，修改后不影响运行）
# ===============================================================
platforms:
  - id: "toutiao"
    name: "今日头条"
  - id: "baidu"
    name: "百度热搜"
  - id: "wallstreetcn-hot"
    name: "华尔街见闻"
  - id: "thepaper"
    name: "澎湃新闻"
  - id: "bilibili-hot-search"
    name: "bilibili 热搜"
  - id: "cls-hot"
    name: "财联社热门"
  - id: "ifeng"
    name: "凤凰网"
  - id: "tieba"
    name: "贴吧"
  - id: "weibo"
    name: "微博"
  - id: "douyin"
    name: "抖音"
  - id: "zhihu"
    name: "知乎"

# ===============================================================
# 3. 数据源 - RSS 订阅
#
# 与热榜数据分开存储，按时间流展示
# 每个源配置：id(唯一标识)、name(显示名称)、url(订阅地址)
# enabled: 可选，默认 true
# max_age_days: 可选，覆盖全局 freshness_filter.max_age_days
# ===============================================================
rss:
  enabled: true # 是否启用 RSS 抓取

  # 文章新鲜度过滤配置（全局默认值）
  # 过滤掉发布时间超过指定天数的旧文章，避免同一篇文章重复出现在推送中
  #
  # 过滤逻辑：
  #   - 文章发布时间距当前时间（app.timezone 时区）超过 N 天则不推送
  #   - 无发布时间的文章会被保留（不过滤）
  #
  # ⚠️ 过滤时机：在推送阶段过滤
  #    - 所有文章都会存入数据库（MCP Server 的 AI 查询仍可访问）
  #    - 只有新鲜的文章会被推送到通知渠道
  freshness_filter:
    enabled: true # 是否启用新鲜度过滤（默认启用）
    max_age_days:
      3 # 最大文章年龄（天）
      # - 正整数：只推送 N 天内的文章
      # - 0：禁用过滤，推送所有文章

  # 单个 feed 可配置 max_age_days 覆盖全局设置：
  # - 不配置：使用全局 freshness_filter.max_age_days（默认 3 天）
  # - 正整数：覆盖全局设置，只推送此天数内的文章
  # - 0：禁用此频道的新鲜度过滤，推送所有文章
  feeds:
    - id: "hacker-news"
      name: "Hacker News"
      url: "https://hnrss.org/frontpage"
      # max_age_days: 1               # 示例：只推送1天内的文章

    - id: "ruanyifeng"
      name: "阮一峰的网络日志"
      url: "http://www.ruanyifeng.com/blog/atom.xml"
      # max_age_days: 7               # 示例：推送7天内的文章（更新较慢的博客）

    - id: "yahoo-finance"
      name: "雅虎财经"
      url: "https://finance.yahoo.com/news/rssindex"
      enabled: false # 禁用

    # =============== Web3 信息源 ===============
    #
    # 📋 待接入信息源状态：
    # ✅ Cointelegraph - 已接入 (RSS)
    # ✅ TechFlow 深潮 - 已接入 (Substack RSS)
    # ❌ ME News (MetaEra) - 无 RSS 源，网站使用 JavaScript 渲染，需开发爬虫
    # ❌ ChainCatcher - 无 RSS 源，需开发爬虫
    #
    # ===============================================

    # Cointelegraph - 全球头部加密新闻 ✅
    - id: "cointelegraph"
      name: "Cointelegraph"
      url: "https://cointelegraph.com/rss"
      max_age_days: 3

    # TechFlow 深潮 - 中文深度加密媒体 ✅
    - id: "techflow"
      name: "深潮 TechFlow"
      url: "https://techflowpost.substack.com/feed"
      max_age_days: 7

    # =============== 替代 Web3 中文媒体源 ===============
    # 由于 ME News 和 ChainCatcher 无 RSS，添加以下替代源
    #
    # ⚠️ 以下源经验证不可用，已注释：
    # - Foresight News (foresightnews.pro/feed) - 返回 JavaScript，非有效 RSS
    # - 律动 BlockBeats (theblockbeats.info/rss) - 404 错误
    # - 金色财经 (jinse.cn/rss) - 未验证
    #
    # 如需中文 Web3 资讯，建议使用已接入的 TechFlow 深潮

    # =============== 国际 Web3 媒体源 ===============

    # CoinDesk - 全球加密新闻
    - id: "coindesk"
      name: "CoinDesk"
      url: "https://www.coindesk.com/arc/outboundfeeds/rss/"
      max_age_days: 3

    # The Block - 区块链深度报道
    - id: "theblock"
      name: "The Block"
      url: "https://www.theblock.co/rss.xml"
      max_age_days: 3

    # Decrypt - 加密货币新闻
    - id: "decrypt"
      name: "Decrypt"
      url: "https://decrypt.co/feed"
      max_age_days: 3

    # Bitcoin Magazine - 比特币专业媒体
    - id: "bitcoinmagazine"
      name: "Bitcoin Magazine"
      url: "https://bitcoinmagazine.com/feed"
      max_age_days: 7

    # Blockworks - 机构级加密资讯 ✅ (已验证可用)
    - id: "blockworks"
      name: "Blockworks"
      url: "https://blockworks.co/feed"
      max_age_days: 3

    # 自定义源示例
    # - id: "custom-feed"
    #   name: "自定义源"
    #   url: "https://example.com/feed.xml"
    #   enabled: false
    #   max_age_days: 0               # 示例：禁用过滤，推送所有文章

# ===============================================================
# 4. Web3 信息源爬虫
#
# 用于采集不提供 RSS 的 Web3 媒体网站
# 支持: ChainCatcher (链捕手), ME News (MetaEra)
# 详细配置见: config/web3_sources.yaml
# ===============================================================
web3:
  enabled: true # 是否启用 Web3 爬虫

  # 请求间隔（毫秒）- 建议设置较长间隔避免被封
  request_interval: 3000

  # 请求超时（秒）
  timeout: 30

  # 是否使用代理
  use_proxy: false
  proxy_url: ""

  # 文章新鲜度过滤
  freshness_filter:
    enabled: true
    max_age_days: 3

  # Web3 信息源配置
  feeds:
    # ChainCatcher 链捕手 - 中文区块链资讯平台 ✅
    - id: "chaincatcher"
      name: "ChainCatcher 链捕手"
      url: "https://www.chaincatcher.com/news"
      crawler_type: "chaincatcher"
      max_items: 50
      enabled: true
      max_age_days: 3

    # ME News (原 MetaEra) - 香港 Web3.0 媒体平台
    # ⚠️ 暂不可用：网站需要 JavaScript 渲染，API 也无法访问
    # 需要使用 Selenium/Playwright 等浏览器自动化工具才能抓取
    - id: "menews"
      name: "ME News"
      url: "https://www.me.news/news"
      crawler_type: "menews"
      max_items: 50
      enabled: false # 暂时禁用，需要浏览器渲染支持
      max_age_days: 3

# ===============================================================
# 5. 报告模式
#
# 🔸 daily（当日汇总模式）
#    • 推送时机：按时推送(默认每小时推送一次)
#    • 显示内容：当日所有匹配新闻 + 新增新闻区域
#    • 适用场景：日报总结、全面了解当日热点趋势
#
# 🔸 current（当前榜单模式）
#    • 推送时机：按时推送(默认每小时推送一次)
#    • 显示内容：当前榜单匹配新闻 + 新增新闻区域
#    • 适用场景：实时热点追踪、了解当前最火的内容
#
# 🔸 incremental（增量监控模式）
#    • 推送时机：有新增才推送
#    • 显示内容：新出现的匹配频率词新闻
#    • 适用场景：避免重复信息干扰
# ===============================================================
report:
  mode: "current" # 可选: daily | current | incremental
  display_mode:
    "keyword" # 可选: keyword | platform
    # keyword: 按关键词分组显示（默认）
    # platform: 按平台/来源分组显示
  rank_threshold: 5 # 排名高亮阈值
  sort_by_position_first: false # true=按配置位置排序，false=按热点条数排序
  max_news_per_keyword: 0 # 每个关键词最大显示数量（0=不限制）
  reverse_content_order: false # false=热点词汇统计在前，true=新增热点新闻在前

# ===============================================================
# 6. 推送通知
#
# ⚠️ 重要安全警告 ⚠️
#
# 🔴 请务必妥善保管好 webhooks，不要公开!!!
# 🔴 如果你以 fork 的方式部署在 GitHub 上，请勿在此填写
# 🔴 而是将 webhooks 填入 GitHub Secrets
#    (Settings → Secrets and variables → Actions)
# 🔴 否则：
#    - 轻则：手机上收到大量垃圾广告推送
#    - 重则：webhook 被滥用造成严重安全隐患
#
# 📌 多账号支持说明
#
# • 使用分号(;)分隔多个账号，如："url1;url2;url3"
# • 需要配对的配置（如 Telegram 的 token 和 chat_id）数量必须一致
# • 每个渠道最多支持 max_accounts_per_channel 个账号
# • 邮箱已支持多收件人（逗号分隔）
# ===============================================================
notification:
  enabled: true # 是否启用通知功能（配置好 webhook 后改为 true）

  # 🕐 推送时间窗口控制（可选功能）
  # 用途：限制推送的时间范围，避免非工作时间打扰
  # 适用场景：
  #   - 只想在工作日白天接收推送（如 09:00-18:00）
  #   - 希望在晚上固定时间收到汇总（如 20:00-22:00）
  # 注意：GitHub Actions 执行时间不稳定，时间范围建议至少留足 2 小时
  #       如果想要精准的定时推送，建议使用 Docker 部署在个人服务器上
  push_window:
    enabled: false # 是否启用推送时间窗口控制
    start: "20:00" # 开始时间（北京时间）
    end: "22:00" # 结束时间（北京时间）
    once_per_day: true # true=窗口内只推送一次，false=窗口内每次执行都推送

  # 推送渠道配置
  channels:
    feishu:
      webhook_url: "" # 飞书机器人 webhook URL

    dingtalk:
      webhook_url: "" # 钉钉机器人 webhook URL

    # ─────────────────────────────────────────────────────────
    # 企业微信推送配置（推荐用于微信好友订阅）
    #
    # 📌 配置步骤：
    # 1. 下载企业微信 App，注册/登录
    # 2. 创建群聊，将需要接收信息的好友拉入群
    # 3. 群聊 → 右上角「...」→「群机器人」→「添加机器人」
    # 4. 创建自定义机器人，复制 Webhook URL 填入下方
    #
    # 📌 消息类型说明：
    # - markdown: 群机器人模式，支持 Markdown 格式，适合群聊
    # - text: 纯文本模式，适合企业微信应用推送到个人
    # ─────────────────────────────────────────────────────────
    wework:
      webhook_url: "" # 企业微信机器人 webhook URL（格式：https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=xxx）
      msg_type: "markdown" # 消息类型：markdown(群机器人) | text(个人微信应用)

    telegram:
      bot_token: "" # Telegram Bot Token
      chat_id: "" # Telegram Chat ID

    email:
      from: "" # 发件人邮箱地址
      password: "" # 发件人邮箱密码或授权码
      to: "" # 收件人邮箱，多个用逗号分隔
      smtp_server: "" # SMTP 服务器（可选，留空自动识别）
      smtp_port: "" # SMTP 端口（可选，留空自动识别）

    ntfy:
      server_url: "https://ntfy.sh" # ntfy 服务器地址（可改为自托管）
      topic: "" # ntfy 主题名称
      token: "" # ntfy 访问令牌（可选，用于私有主题）

    bark:
      url: "" # Bark 推送 URL（格式：https://api.day.app/your_device_key）

    slack:
      webhook_url: "" # Slack Incoming Webhook URL

# ===============================================================
# 7. 存储配置
# ===============================================================
storage:
  # 存储后端选择
  # - auto: 自动选择（GitHub Actions 且配置了远程存储 → remote，否则 → local）
  # - local: 本地 SQLite + TXT/HTML 文件
  # - remote: 远程云存储（S3 兼容协议，支持 R2/OSS/COS 等）
  backend: "auto"

  # 数据格式选项
  formats:
    sqlite: true # 主存储（必须启用）
    txt: false # 是否生成 TXT 快照
    html: true # 是否生成 HTML 报告（⚠️ 邮件推送必须设为 true）

  # 本地存储配置
  local:
    data_dir: "output" # 数据目录
    retention_days: 0 # 保留天数（0=永久保留）

  # 远程存储配置（S3 兼容协议）
  # 支持: Cloudflare R2, 阿里云 OSS, 腾讯云 COS, AWS S3, MinIO 等
  # 建议将敏感信息配置在 GitHub Secrets 或环境变量中
  remote:
    retention_days: 0 # 保留天数（0=永久保留）
    # S3 兼容配置（或使用环境变量 S3_ENDPOINT_URL 等）
    endpoint_url:
      "" # 服务端点
      # Cloudflare R2: https://<account_id>.r2.cloudflarestorage.com
      # 阿里云 OSS: https://oss-cn-hangzhou.aliyuncs.com
      # 腾讯云 COS: https://cos.ap-guangzhou.myqcloud.com
    bucket_name: "" # 存储桶名称
    access_key_id: "" # 访问密钥 ID
    secret_access_key: "" # 访问密钥
    region: "" # 区域（可选，部分服务商需要）

  # 数据拉取配置（从远程同步到本地）
  # 用于 MCP Server 等场景：爬虫存到远程，MCP 拉取到本地分析
  pull:
    enabled: false # 是否启用启动时自动拉取
    days: 7 # 拉取最近 N 天的数据

# ===============================================================
# 8. 高级设置（一般无需修改）
# ===============================================================
advanced:
  # 版本检查
  version_check_url: "https://raw.githubusercontent.com/sansan0/TrendRadar/refs/heads/master/version"

  # 爬虫设置
  crawler:
    enabled: true # 是否启用爬取新闻功能
    request_interval: 1000 # 请求间隔（毫秒）
    use_proxy: false # 是否启用代理
    default_proxy: "http://127.0.0.1:10801"

  # RSS 设置
  rss:
    request_interval: 2000 # 请求间隔（毫秒）
    timeout: 15 # 请求超时（秒）
    use_proxy: false # 是否使用代理
    proxy_url: "" # RSS 专属代理（留空则使用 crawler.default_proxy）
    notification_enabled: true # 是否启用 RSS 通知推送

  # 排序权重（用于重新排序不同平台的热搜）
  # 合起来等于 1
  weight:
    rank: 0.6 # 排名权重
    frequency: 0.3 # 频次权重
    hotness: 0.1 # 热度权重

  # 多账号限制
  max_accounts_per_channel: 3 # 每个渠道最大账号数量

  # 消息分批大小（字节）- 内部配置，请勿修改
  batch_size:
    default: 4000
    dingtalk: 20000
    feishu: 30000
    bark: 4000
    slack: 4000
  batch_send_interval: 3 # 批次发送间隔（秒）
  feishu_message_separator: "━━━━━━━━━━━━━━━━━━━"
